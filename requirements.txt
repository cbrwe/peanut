flask>=3.0
Pillow>=10.0
pillow-heif>=0.13
imagehash>=4.3
numpy>=1.24
tqdm>=4.65
send2trash>=1.8
python-docx>=1.1
python-pptx>=0.6
openpyxl>=3.1
PyPDF2>=3.0
# Native desktop window (optional for source/dev — required for the bundled
# .app/.exe distribution to provide real native drag-and-drop). If not
# installed, peanut_desktop.py falls back to opening the system browser.
pywebview>=5.0
# pywebview backends — platform-specific (only the relevant one is used)
pyobjc-framework-Cocoa>=9.2; sys_platform == "darwin"
pyobjc-framework-WebKit>=9.2; sys_platform == "darwin"
